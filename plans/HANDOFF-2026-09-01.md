# Handoff: 2026-09-01 execution round (plans 040-055)

Written when the first executor session hit its usage limit. Everything below
is verified against the repository at the moment of writing.

## State of `main`

- `main` is at `5ab59df`. Full suite on `main`: `Executed 331 tests, with 0 failures`.
- Eight plans are merged with merge commits, one branch each, verified after every merge:

| Plan | Branch | Merge sha | Tests on main after merge |
|------|--------|-----------|---------------------------|
| 040 | `advisor/040-ci-macos-26` | `0aac6bf` | 278/0 |
| 046 | `advisor/046-duplicate-handles` | `bf77d82` | 281/0 |
| 041 | `advisor/041-characterization-tests` | `27c7ae5` | 313/0 (1 skipped by design) |
| 044 | `advisor/044-asynctimeout-gate` | `35de66d` | 316/0 |
| 043 | `advisor/043-phone-normalization` | `373b5b8` | 320/0 |
| 052 | `advisor/052-version-dx` | `e4499d3` | 321/0 |
| 049 | `advisor/049-security-hygiene` | `9c1f8db` | 325/0 |
| 042 | `advisor/042-argument-clamps` | `5ab59df` | 331/0 |

- Nothing has been pushed. No PRs opened. `plans/` is still untracked in the
  working tree (`plans/README.md` modified, `plans/04*.md`, `plans/05*.md`, and
  this file untracked). Commit them or leave them; branches never touch `plans/`.
- `plans/README.md` status table is current: DONE rows for the eight above,
  IN PROGRESS rows for 047/048/050, TODO for the rest.

## Remaining plans (8)

| Plan | Depends on | Status |
|------|------------|--------|
| 047 | 041 (merged) | IN PROGRESS. Branch `advisor/047-hot-path-costs` has one commit `f03b4c1` ("perf: encode tool responses in one pass with stable key order", Steps 1-2: golden test + encoder rewrite). Uncommitted edits to `swift/Sources/iMessageMax/Enrichment/ImageProcessor.swift` and `swift/Sources/iMessageMax/Utilities/AppleScript.swift` sit in worktree `.claude/worktrees/agent-a9f28a8f77fd1f49c` (Steps 3-4 partly done, unverified). Either resume there from Step 3 after reviewing the diff, or `git checkout advisor/047-hot-path-costs` fresh and redo Steps 3-4. The branch was cut from `61e75d9`+041; rebase onto `main` before merging (049 also touched `AppleScript.swift`). |
| 048 | 040, 044 (merged) | IN PROGRESS, nothing committed. Branch `advisor/048-deps-and-floor` at `9c1f8db`; drift check had passed. Restart from the plan; cut from current `main`. Needs network for `swift package update`. |
| 050 | 044 (merged) | IN PROGRESS, nothing committed. Branch `advisor/050-lifecycle-leaks` at `e4499d3`; drift check had passed. Restart from the plan; cut from current `main`. |
| 045 | 041, 042 (merged) | TODO. Search lane; must land before 051. |
| 051 | 042, 045 | TODO. After 045. |
| 053 | 048 | TODO. |
| 055 | 048, 051 | TODO. |
| 054 | 041, 042, 045, 046, 049, 051 | TODO. Last, alone. Pure refactor; any pre-existing test edit is a STOP. |

Recommended order: 047, 050, 048 (parallel worktrees, merge one at a time),
then 045 -> 051, then 053 + 055, then 054.

## Worktrees left behind

`git worktree list` shows one worktree per branch under `.claude/worktrees/`.
The eight merged ones can be removed (`git worktree remove <path>`); their
branches may be deleted after that. Keep `agent-a9f28a8f77fd1f49c` (047 WIP)
until its uncommitted changes are reviewed. The 048 and 050 worktrees are
clean and disposable.

Caveat observed this round: worktrees created by the executor harness were
sometimes cut from a stale commit (`61e75d9`), not the current `main`. Every
executor must run `git log --oneline -1 main` first and branch from `main`.

## Findings from executors that no plan covers

- `swift/Tests/iMessageMaxTests/AsyncTimeoutTests.swift:18-26`: the pre-arm
  cancellation test hangs (rather than failing in 2 s) if the 044 bug is ever
  reintroduced, because `withTaskGroup` waits for the un-cancellable child.
  Worth a bounded expectation in a later pass.
- `swift/Sources/iMessageMax/Tools/SendResolution.swift:97`: after 043, a bare
  5-8 digit `to` routes to the phone branch and returns "Invalid phone number
  format" instead of falling through to contact-name lookup; a `to` with
  letters plus 10+ digits now goes to contact lookup. Intended, but user-visible.
- `swift/Sources/iMessageMax/Utilities/IdentityDisplayFormatter.swift:39-47`:
  with a duplicated handle reaching the formatter through one of the ten
  still-undeduped participant query sites, the same person is disambiguated
  against themself ("Alice Smith (0001)"). Plan 054's shared helper removes it.
- `swift/Sources/iMessageMax/Tools/GetMessagesInternals.swift:130`:
  `parseChatId` never accepted a bare numeric chat id (only `chatNN`), so a
  bare `"20"` still takes the GUID LIKE fallback. Plan 054 territory.
- `swift/Sources/iMessageMax/Tools/SearchInternals.swift:133`: a constant
  `LIKE ?` with `%http%` and no ESCAPE; not user input, but any
  "every LIKE has ESCAPE" grep needs an allowlist for it.
- `swift/Sources/iMessageMax/Tools/Search.swift:237`: the argument clamp lives
  inside the `registerTool` closure, not the static `execute`, so tests that
  call the static function bypass it (`ArgumentClampTests` goes through the
  registry on purpose).
- `.github/workflows/build.yml:47`: 052's version check step runs before the
  build (fail-fast); the plan's wording was ambiguous about placement.
- CI has not yet been observed green: 040's Step 5 (push the branch, watch
  `build.yml`, dispatch `release.yml`) is an operator action still pending.

import Foundation
import MCP

enum ToolRegistry {
    static func registerAll(on server: Server) {
        // Tools will be registered here as they are implemented
        // Placeholder - each tool will add its registration
    }
}
